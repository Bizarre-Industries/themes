# Bizarre Industries — Vim

`BZR / VIM / V0.1`

Classic Vim colorschemes (also work in Neovim if you don't want the Lua plugin).

```bash
mkdir -p ~/.vim/colors
cp editors/vim/colors/*.vim ~/.vim/colors/
```

In `.vimrc`:
```vim
set termguicolors
colorscheme bizarre-void
```

CATCH THE STARS.
